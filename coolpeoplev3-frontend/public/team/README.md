Square headshots for the /fund team cards.

Drop a file here (400x400 or larger, square, .jpg or .webp), then set the
matching `photo` field in TEAM in src/assets/pages/fund/Fund.jsx:

    photo: '/team/lorenzo.jpg'

Leave `photo` unset and the card falls back to gold initials, which is a
finished state rather than a placeholder.
